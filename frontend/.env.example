# URL del backend Express (sin barra final). Por defecto en código: http://localhost:3000
VITE_API_BASE_URL=http://localhost:3000

# Docker: Vite inyecta esta variable en BUILD TIME. En compose se pasa como build-arg
# desde el .env de la raíz del repo (ver README raíz).
