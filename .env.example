# Docker Compose: URL del API tal como la alcanza el navegador (build-time de Vite en el contenedor frontend).
# Ejemplo local con puertos publicados por compose: backend en 3000, frontend en 8080.
VITE_API_BASE_URL=http://localhost:3000

# Puertos publicados en el host (opcional)
# WHATSAPP_BOT_PORT=3000
# FRONTEND_PORT=8080
